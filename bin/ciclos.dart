void main(List<String> args) {
  int num = 7;
  int factorial = 1;

  while (num >= 1) {
    factorial = factorial * num;
  }
  print('El factorail es:$factorial');

  num = 5;
  int contador = 1;

  while (contador <= 10) {}

  // List<String> alumnos = ['Juan', 'pedro', 'maria', 'dulce'];
  // alumnos.add('Yo merengues');
  // alumnos.insert(2, 'lukas');
  // alumnos.removeLast();

  // for (int pos = 0; pos < alumnos.length; pos++) {
  //   print(alumnos[pos]);
  // }
}
